"""Дефолтные значения настроек и промптов. Идемпотентна: существующие
записи не перезаписываются — отредактированный в админке промпт переживает
перезапуск сервиса."""

from sqlalchemy.exc import IntegrityError
from sqlalchemy.orm import Session

from app.models.setting import Setting

# Ключ RouterAI сюда не входит: у него нет осмысленного дефолта, он вводится
# админом и хранится зашифрованным.
DEFAULT_SETTINGS = {
    "routerai_base_url": "https://routerai.ru/api/v1",
    "text_model": "anthropic/claude-sonnet-4-6",
    "image_model": "openai/gpt-image-2",
    "image_quality": "medium",   # high дороже втрое: ≈16.8 против ≈5.4 за кадр
    "image_size": "1536x1024",
    "image_workers": "4",
    "llm_max_retries": "3",
    # Квота Yandex Search API на статистику Wordstat — 100 запросов в час
    # (проверено 2026-09-16). Выше — только если Яндекс поднимет квоту.
    "wordstat_hourly_limit": "100",
    # Через запятую; сравнение по подстроке без учёта регистра. Фразы с этими
    # словами не попадают ни в промпт, ни в теги.
    "meta_stoplist": ("леруа, мерлен, лемана, петрович, авито, озон, ozon, wildberries, "
                      "вайлдберриз, яндекс маркет, своими руками"),
}

SECRET_KEYS = {"routerai_api_key", "wordstat_api_key"}

# int-настройки валидируются на PUT до записи (см. admin_settings.py) —
# иначе опечатка проходит с 200 и падает позже необработанным ValueError
# внутри celery-таски (Task 8), где её уже никто не увидит.
INT_KEYS = {"image_workers", "llm_max_retries", "wordstat_hourly_limit"}

# Диапазоны для тех int-настроек, которым мало быть просто целым числом.
# image_workers — ширина ThreadPoolExecutor(max_workers=...) в Task 8/16:
# 0 и отрицательные валят Celery-таску необработанным ValueError (max_workers
# must be greater than 0), а сверху границы не было вовсе — опечатка вроде
# «40» вместо «4» линейно растит число потоков и память на каждую партию
# статей. 8 — с запасом выше практического максимума, 1 — минимум, при
# котором параллелизм просто вырождается в последовательную генерацию.
# Данные, а не условие в роутере: следующие настройки с границами
# добавляются сюда же, без правки app/api/admin_settings.py.
# Верхняя граница llm_max_retries — это app.ai.factory.SETTING_MAX_RETRIES.
# Держим здесь литералом, а не импортом: factory сам импортирует этот модуль
# (DEFAULT_SETTINGS, INT_RANGES), и обратная ссылка дала бы цикл. От расхождения
# двух чисел страхует test_admin_range_matches_factory_ceiling в
# tests/test_ai_factory.py, а не дисциплина.
# Без этой границы админка принимала любое целое: значение уезжало в БД с 200,
# а код молча срезал его до потолка, сообщая об этом только в лог воркера —
# то есть настройка показывала одно, а система делала другое (2026-09-12).
INT_RANGES = {"image_workers": (1, 8), "llm_max_retries": (1, 3),
              "wordstat_hourly_limit": (1, 100000)}


def seed_settings(db: Session) -> None:
    for key, value in DEFAULT_SETTINGS.items():
        if db.get(Setting, key) is None:
            db.add(Setting(key=key, value=value, is_secret=False))
    try:
        db.commit()
    except IntegrityError:
        # Конкурентный seed_settings: вызывается на каждом GET
        # /api/admin/settings, так что два админа, открывшие страницу
        # настроек на пустой БД одновременно, оба проходят SELECT-фазу
        # (видят пусто) раньше, чем кто-то из них коммитит — тот же класс
        # гонки, что чинили в Task 5 для SettingsService._upsert. Один из
        # них коммитит первым и захватывает часть или все дефолтные ключи;
        # наш commit() ловит конфликт первичного ключа. Откатываем и
        # смотрим заново: то, что конкурент уже вставил, теперь видно и не
        # добавляется повторно (идемпотентность), то, что всё ещё
        # отсутствует — довставляем и коммитим один раз.
        db.rollback()
        for key, value in DEFAULT_SETTINGS.items():
            if db.get(Setting, key) is None:
                db.add(Setting(key=key, value=value, is_secret=False))
        db.commit()


from app.ai.prompts import PROMPT_KEYS  # noqa: E402
from app.models.prompt_template import PromptTemplate  # noqa: E402

DEFAULT_PROMPTS = {
    # Тематику задаёт site_description, а не домен и не список прошлых статей:
    # по ним модель промахивается — у стройбазы и у производителя смесей рубрика
    # называется одинаково, а нужные темы разные.
    "topics": """Ты редактор блога сайта «{{ site_name }}».

О сайте и его аудитории:
{{ site_description }}

Тон материалов: {{ tone_of_voice }}

Предложи {{ count }} тем для полезных статей. Требования:
- тема попадает в тематику сайта и отвечает на реальный вопрос его аудитории;
- заголовок 40–80 символов, без кликбейта и без года в тексте;
- темы не должны повторять и близко пересекаться с уже опубликованными.

Уже опубликованные статьи:
{% for title in existing_titles %}- {{ title }}
{% endfor %}

Верни СТРОГО JSON-массив строк без пояснений, например:
["Как выбрать фундамент для дома на глинистой почве", "Чем утеплять каркасный дом"]""",

    "article_body": """Напиши полезную статью на тему «{{ topic }}» для сайта
«{{ site_name }}».

О сайте и его аудитории:
{{ site_description }}

Тон материала: {{ tone_of_voice }}

Требования к тексту:
- объём 2500–3000 символов;
- практическая польза, конкретика, без воды и рекламных обещаний;
- никаких упоминаний цен, акций и сроков — они устаревают;
- ровно {{ image_count }} иллюстраций внутри текста.

Требования к разметке: точно повтори структуру, набор тегов и CSS-классы
эталонной статьи этого сайта, приведённой ниже. Не добавляй своих классов,
не подключай стили и скрипты.

Иллюстрации вставь тегами <img> ровно с этими путями, по одному на смысловой блок:
{% for path in image_paths %}- {{ path }}
{% endfor %}

Эталонная статья (образец разметки, НЕ образец темы):
{{ reference_html }}

Верни СТРОГО JSON-объект:
{"title": "...", "html": "...", "meta_description": "...", "meta_keywords": "..."}""",

    "article_label": """Выбери рубрику для статьи «{{ title }}» (тема: {{ topic }}).

Доступные рубрики:
{% for label in labels %}- {{ label }}
{% endfor %}

Выбери ОДНУ, наиболее подходящую по смыслу. Новых не придумывай: список
закрытый. Верни только её название одной строкой, слово в слово, без кавычек
и пояснений.""",

    "cover": """Составь промпт для генератора изображений — обложка статьи
«{{ topic }}».

Стиль обложки: {{ cover_style }}

Обложка — витрина статьи: общий план, узнаваемый сюжет, никакого текста и
надписей на изображении. Верни только сам промпт одной строкой, без пояснений.""",

    "content_image": """Составь промпт для генератора изображений — иллюстрация
к фрагменту статьи «{{ topic }}».

Фрагмент: {{ paragraph }}

Стиль: {{ image_style }}

Изображение поясняет смысл фрагмента. Без текста и надписей на изображении.
Верни только сам промпт одной строкой, без пояснений.""",

    "builder_text": """Напиши маркетинговый текст о компании «{{ company_name }}»
({{ category }}, {{ city }}) для страницы на сайте «{{ site_name }}».

Тон материала: {{ tone_of_voice }}

Текст сайта компании (сырой, может содержать мусор навигации):
{{ scraped_text }}

Требования:
- пиши от третьего лица о компании «{{ company_name }}» — сайт «{{ site_name }}»
  каталог, рассказывающий о СТОРОННИХ компаниях, а не сайт самой компании;
  «компания выполняет работы», «предлагает», «строит», а НЕ «мы выполняем
  работы», «предлагаем», «строим»; даже если исходный текст сайта компании
  написан от первого лица («мы», «наша компания») — пересказывай его в
  третьем лице;
- используй факты из текста сайта, не выдумывай услуги и проекты, которых там нет;
- если текста сайта недостаточно, чтобы содержательно заполнить какое-то поле,
  оставь это поле короче или общим по смыслу — не выдумывай факты, чтобы
  дотянуть до объёма;
- никаких цен, контактов и юридических реквизитов — они уже есть на странице отдельно;
- деловой, но не рекламный тон, без превосходной степени без оснований;
- объём (см. directions/content-standards.md): about_company — 600-1000 символов,
  3-4 абзаца; specialization, projects_services и benefits — по 300-500 символов
  каждое, 2-3 абзаца. Это ориентир, небольшой выход за границы допустим.

Верни СТРОГО JSON-объект с пятью полями:
{"about_company": "...", "specialization": "...", "projects_services": "...",
 "benefits": "...", "city_prepositional": "..."}

city_prepositional — название города «{{ city }}» в предложном падеже, как
оно встанет во фразу «компания работает в ...»: Москва → Москве, Тверь →
Твери, Ярославль → Ярославле, «рабочий посёлок Нахабино» → «рабочем посёлке
Нахабино». Только форма названия, без предлога.""",

    # Метатеги категорий (directions/2026-09-16-category-meta-design.md). Фраза
    # и формы — отдельным вызовом на всё дерево: по ним код ходит в Wordstat.
    "category_seeds": """Ты SEO-специалист интернет-магазина «{{ site_name }}».

О сайте:
{{ site_description }}

Ниже категории каталога: id и путь в дереве. Для каждой категории определи,
как её товары называют и ищут в Яндексе, и верни варианты названия с формами.
Какой вариант ищут чаще, проверит Wordstat — твоя задача не пропустить
настоящие варианты.

Правила:
- variants — от 1 до 4 вариантов названия ОДНОГО И ТОГО ЖЕ товара: название из
  каталога в поисковом виде, общеупотребительное название, аббревиатура или
  расшифровка. Пример: «ГКЛ» → «гипсокартон», «гкл», «гипсокартонный лист».
  Если другого названия у товара нет — один вариант;
- только ТОТ ЖЕ САМЫЙ товар: не давай более широкий или соседний товар (для
  «Профиль для гипсокартона» нельзя «гипсокартон»), отдельный вид из категории
  (для «Блок строительный» нельзя «пеноблок») и многозначные аббревиатуры
  («псб» чаще ищут как банк);
- phrase — короткая фраза в том виде, в каком её вводят в поиск: обычный
  порядок слов («металлический уголок», а не «уголок металлический»), без
  перечислений из названия раздела (для «Кирпич Блоки Тротуар» выбери главный
  товар по смыслу пути); бренд оставляй, если он есть в названии категории;
- nominative — phrase в именительном падеже;
- buy — «купить» + phrase в винительном падеже: «купить фанеру», «купить
  металлический уголок»;
- price — «цена» + phrase в родительном падеже: «цена фанеры»;
- всё в нижнем регистре, кроме брендов латиницей.

Категории:
{% for line in categories %}{{ line }}
{% endfor %}
Верни СТРОГО JSON-массив объектов, по одному на каждую категорию, без пояснений:
[{"id": 39, "variants": [
  {"phrase": "гипсокартон", "nominative": "гипсокартон", "buy": "купить гипсокартон", "price": "цена гипсокартона"},
  {"phrase": "гкл", "nominative": "гкл", "buy": "купить гкл", "price": "цена гкл"}]},
 {"id": 46, "variants": [
  {"phrase": "фанера", "nominative": "фанера", "buy": "купить фанеру", "price": "цена фанеры"}]}]""",

    # Проверка смысла вариантов названия по запросам Wordstat — только когда
    # чаще ищут не первый вариант (app/category_meta/generator.py). Решение
    # владельца 2026-09-16: строго тот же товар.
    "category_variants": """Категория каталога интернет-магазина «{{ site_name }}»: {{ category_name }}
(путь в каталоге: {{ category_path }}).

Ниже варианты поискового названия этой категории — у каждого число запросов в
месяц и частые запросы из Яндекс Wordstat. Определи, какие варианты означают
ТОТ ЖЕ САМЫЙ товар, что и категория.

Не подходят:
- другое значение слова или аббревиатуры — смотри на запросы: «псб банк»,
  «псб онлайн» говорят, что «псб» ищут как банк, а не как пенополистирол;
- отдельный вид товара из категории: «тротуарная плитка» для «Благоустройство»,
  «пеноблок» для «Блок строительный»;
- более широкий или соседний товар: «кровельный саморез» для «Саморез для
  сэндвич-панелей».

Варианты:
{% for line in variants %}{{ line }}
{% endfor %}
Верни СТРОГО JSON-объект без пояснений, варианты — дословно как в списке:
{"same_product": ["вариант", "вариант"]}""",

    # Форму названия выбирает код по Wordstat (chosen_form) — модель её не меняет.
    # Правила полей дублируют проверку app/category_meta/validate.py: нарушение
    # всё равно вернётся модели списком violations на второй попытке.
    "category_meta": """Ты SEO-специалист интернет-магазина «{{ site_name }}». Напиши
метатеги страницы категории каталога.

О сайте (единственный источник фактов о магазине — доставка, оплата и т.п.):
{{ site_description }}

Категория: {{ category_name }} (путь: {{ category_path }})
Поисковая фраза: {{ form_nominative }}; «купить»: {{ form_buy }}; «цена»: {{ form_price }}
Город: {{ city_in }}. Бренд: {{ brand }}.
Запросов по фразе в городе за 30 дней: {{ total_count }}.

Как ищут (Wordstat, фраза — частота в месяц):
{% for line in phrases %}{{ line }}
{% endfor %}{% if alternatives %}
Другие названия этих товаров (ищут реже, чем «{{ form_nominative }}») — всего запросов и частые фразы:
{% for line in alternatives %}{{ line }}
{% endfor %}{% endif %}
Форма названия выбрана по статистике — используй её, не меняй:
{% if chosen_form == "nominative" %}- чаще ищут без склонения: начни title с «{{ form_nominative }}» (с заглавной буквы) и построй фразу так, чтобы она читалась корректно, например «{{ form_nominative }} {{ city_in }} — купить по выгодной цене | {{ brand }}».
{% else %}- чаще ищут со склонением: начни title с «{{ form_buy }}» (с заглавной буквы), например «{{ form_buy }} {{ city_in }} по выгодной цене | {{ brand }}».
{% endif %}Продающее слово, которое чаще встречается в запросах: «{{ sell_word }}».

Требования:
- title: до 70 символов, оканчивается на « | {{ brand }}», содержит «{{ city_in }}» и слово «купить» или «цена»;
- h1: до 60 символов, фраза и город («{{ form_nominative }} {{ city_in }}», с заглавной буквы), без бренда, не повторяет title;
- meta_description: 140–170 символов, содержит «{{ city_in }}» и фразу; продающие слова из статистики и виды товара из запросов; не пиши «в наличии», «скидки», «оптом» и другие факты о магазине, которых нет в описании сайта;
- meta_keywords: до 10 фраз через запятую в нижнем регистре, ТОЛЬКО дословно из запросов выше (включая фразы других названий) или «{{ form_nominative }} {{ city_in }}»; сначала коммерческие (купить, цена), затем виды товара;
- ai_keywords: 10–15 фраз через запятую — как человек спросил бы нейросеть о выборе и покупке этого товара: «где купить … {{ city_in }}», «какая … подходит для …», виды и назначения из запросов.
{% if violations %}
Предыдущий вариант не прошёл проверку. Исправь:
{% for item in violations %}- {{ item }}
{% endfor %}{% endif %}
Верни СТРОГО JSON-объект без пояснений:
{"title": "...", "h1": "...", "meta_description": "...", "meta_keywords": "...", "ai_keywords": "..."}""",

    # Правила дублируют app/category_meta/seo_text.py — нарушения вернутся
    # модели списком violations на второй попытке.
    "category_seo_text": """Ты SEO-копирайтер интернет-магазина «{{ site_name }}». Напиши SEO-текст
для страницы категории каталога — он стоит под списком товаров.

О сайте (единственный источник фактов о магазине — доставка, оплата и т.п.):
{{ site_description }}

Категория: {{ category_name }} (путь: {{ category_path }})
Поисковая фраза: {{ form_nominative }}. Город: {{ city_in }}.
Заголовок страницы (h1): {{ h1 }}

Ключевые слова страницы:
{% for phrase in keywords %}{{ phrase }}
{% endfor %}
Как ищут (Wordstat, фраза — частота в месяц):
{% for line in phrases %}{{ line }}
{% endfor %}{% if products %}
Товары в категории (для примеров видов, марок и размеров):
{% for name in products %}{{ name }}
{% endfor %}{% endif %}
Требования:
- объём — около 2200 символов видимого текста (без тегов), примерно 300 слов: 3–4 блока по 1–2 коротких абзаца; не растягивай;
- HTML: у каждого подзаголовок <h2>, дальше абзацы <p>, где уместно — список <ul><li>; других тегов, атрибутов, ссылок и заголовка <h1> не используй;
- фраза «{{ form_nominative }}» и «{{ city_in }}» встречаются в тексте естественно, ключевые слова выше распредели по тексту без переспама, склоняй их по смыслу;
- о чём писать: какие бывают виды и для чего они подходят, как выбрать, на что смотреть при покупке, примеры товаров из списка (марки, размеры) — без цен;
- не пиши «в наличии», «скидки», «оптом», сроки и условия доставки и другие факты о магазине, которых нет в описании сайта; не выдумывай характеристики конкретных товаров;
- пиши для покупателя, живым языком, без воды и штампов вроде «в современном мире».
{% if violations %}
Предыдущий вариант не прошёл проверку. Исправь:
{% for item in violations %}- {{ item }}
{% endfor %}Объём держи тот же — около 2200 символов, примерно 300 слов: не длиннее и не короче.
{% endif %}
Верни только HTML текста — без пояснений, без JSON и без ```.""",
}


def _add_missing_prompts(db: Session) -> None:
    existing = {
        row.key for row in
        db.query(PromptTemplate).filter(PromptTemplate.site_id.is_(None)).all()
    }
    for key in PROMPT_KEYS:
        if key not in existing:
            db.add(PromptTemplate(key=key, site_id=None, text=DEFAULT_PROMPTS[key]))


def seed_prompts(db: Session) -> None:
    """Идемпотентна: отредактированный в админке промпт не перезаписывается."""
    _add_missing_prompts(db)
    try:
        db.commit()
    except IntegrityError:
        # Тот же конкурентный сценарий, что и в seed_settings выше: Task 13
        # зовёт seed_prompts на каждом GET /api/admin/prompts, поэтому два
        # админа, открывшие экран промптов на пустой БД, оба проходят
        # SELECT-фазу (видят пусто) раньше, чем кто-то из них коммитит.
        # Второй ловит конфликт частичного уникального индекса
        # uq_prompt_key_global (Task 12) — проверено на живом Postgres 16:
        # без этой ветки его GET отвечает 500. После rollback вставленное
        # конкурентом видно, поэтому повторный проход добавляет только
        # действительно недостающее.
        db.rollback()
        _add_missing_prompts(db)
        db.commit()
